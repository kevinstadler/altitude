{
  "name": "ol-parcel",
  "version": "1.0.0",
  "homepage": "https://kevinstadler.github.io/altitude/",
  "description": "Example using OpenLayers with Parcel",
  "scripts": {
    "start": "parcel index.html --open",
    "predeploy": "parcel build --no-source-maps --public-url . index.html",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^3.1.0",
    "ol": "^6.5.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.2"
  }
}
